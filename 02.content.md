---
name: content_page
---

# Equations, figures, footnotes & comments

Content inside the panels behaves like any other MyST content — labels
resolve, equations and figures number, footnotes collect — but the plugin's
transforms apply the `.old`/`.new` contract so the two documents stay in
lockstep.

## Equations & figures

- Equations labeled with a shared stem — `\label{eq_period.old}` in the
  original, `\label{eq_period.new}` in the translation — display the **same
  number** in both panels, and each pair consumes exactly one slot in the
  page-wide sequence.
- Unpaired or unlabeled equations inside the panels are left **unnumbered**
  (the second equation below demonstrates this).
- Figures work differently, because a translation may legitimately drop or
  add figures, and an unnumbered figure is awkward: labeled panel figures are
  numbered **by side**, with slots allocated by walking the two panels in
  step. Matched pairs share their index — *Figure O.1* ↔ *Figure N.1* and
  *Figure O.3* ↔ *Figure N.3* below — while a figure without a counterpart
  takes its slot at its own position: the French edition inserts
  *Figure N.2*, so the old panel skips from O.1 to O.3 (the gap says "no
  counterpart here"), and both panels stay in reading order. A label needs
  no suffix to be numbered — only pairing uses the `.old`/`.new` stems.
  Unlabeled figures stay unnumbered.
- Cross-references work against either side: @eq_period.old and
  @eq_period.new render the same number, and @fig_cover.old,
  @fig_cover.new, and @fig_detail resolve to the side-tagged figure
  numbers.

## Footnotes & comments

Footnotes follow the contract too:

- Matched labels — `[^condition.old]` ↔ `[^condition.new]` — are a translated
  pair: they share a number, tagged by side (**O.1** / **N.1**), and are listed
  in the **Footnotes** section at the bottom of the page.
- Footnotes *without* a counterpart are treated as translator **comments**:
  they number sequentially as **C.1, C.2, …** and collect in their own
  **Comments** section below the footnotes. In the demo below, the original
  carries an editorial comment and the translation a translator's note.
- Footnotes on the host page itself[^host] keep their plain numbers.
- Hover any marker for a popover; click it to jump to the entry.

[^host]: Like this one, written on the page rather than inside a panel.

:::{side-by-side}
:old: ./old/02.snippet_example_old.md
:new: ./new/02.snippet_example_new.md
:max-height: none
:old-title: English — collector's notebook
:new-title: Français — carnet de collectionneur
:::

## Multiple blocks per page

Numbering continues across side-by-side blocks: the pair below shows the
page's second equation number — compare @eq_coda.old and @eq_coda.new. This
block also demonstrates `:split: 30%` for an uneven initial split.

:::{side-by-side}
:old: ./old/03.snippet_example_old.md
:new: ./new/03.snippet_example_new.md
:split: 30%
:max-height: none
:old-title: English (original)
:new-title: Français (traduction)
:::
