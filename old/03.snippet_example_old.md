(coda.old)=
Equation numbering continues across every side-by-side block on the page: the
matched pair below is the second one, so both panels display the same second
number.

$$
f = \frac{1}{T} \approx 0.56\ \text{Hz} \label{eq_coda.old}
$$
